<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fengdu.dao.TCategoryDao">

    <resultMap type="com.fengdu.entity.TCategoryEntity" id="tCategoryMap">
        <result property="id" column="id"/>
        <result property="categoryName" column="category_name"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="createTime" column="create_time"/>
        <result property="modifyTime" column="modify_time"/>
        <result property="creator" column="creator"/>
        <result property="modifier" column="modifier"/>
    </resultMap>

	<select id="queryObject" resultType="com.fengdu.entity.TCategoryEntity">
		select
			c.`id`,
			c.`category_name`,
			c.`merchant_id`,
			c.`create_time`,
			c.`modify_time`,
			c.`creator`,
			c.`modifier`
		from t_category c,t_merchant m 
		where c.id = #{id} and c.`merchant_id` = m.id
	</select>

	<select id="queryList" resultType="com.fengdu.entity.TCategoryEntity">
		select
    		c.`id`,
			c.`category_name`,
			c.`merchant_id`,
			c.`create_time`,
			c.`modify_time`,
			c.`creator`,
			c.`modifier`
		from t_category c,t_merchant m 
		WHERE c.`merchant_id` = m.id
		<if test="name != null and name.trim() != ''">
			AND c.`category_name` LIKE concat('%',#{name},'%')
		</if>
		<if test="merchantId != null">
			AND c.merchant_id = #{merchantId}
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by c.id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from t_category
		WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
        <if test="merchantId != null">
			AND merchant_id = #{merchantId}
		</if>
	</select>
	 
	<insert id="save" parameterType="com.fengdu.entity.TCategoryEntity" useGeneratedKeys="true" keyProperty="id">
		insert into t_category(
			`category_name`,
			`merchant_id`,
			`create_time`,
			`modify_time`,
			`creator`,
			`modifier`)
		values(
			#{categoryName},
			#{merchantId},
			#{createTime},
			#{modifyTime},
			#{creator},
			#{modifier})
	</insert>
	 
	<update id="update" parameterType="com.fengdu.entity.TCategoryEntity">
		update t_category 
		<set>
			<if test="categoryName != null">`category_name` = #{categoryName}, </if>
			<if test="merchantId != null">`merchant_id` = #{merchantId}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="modifyTime != null">`modify_time` = #{modifyTime}, </if>
			<if test="creator != null">`creator` = #{creator}, </if>
			<if test="modifier != null">`modifier` = #{modifier}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from t_category where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from t_category where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>